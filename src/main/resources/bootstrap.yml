spring:

  application:
    name: api-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true

    config:
      uri: http://vtsign.tech:8888
server:
  port: 8765
springdoc:
  swagger-ui:
    path: /api-docs
  version: '1.0'
